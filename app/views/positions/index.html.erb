<main class="app-content">
  <div class="app-title">
    <div>
      <h1><i class="fa fa-dashboard"></i> <%= t('ui.position') -%></h1>
      <p><%= t('ui.position_description') -%></p>
    </div>
    <ul class="app-breadcrumb breadcrumb">
      <li class="breadcrumb-item"><i class="fa fa-home fa-lg"></i></li>
      <li class="breadcrumb-item"><a href="<%= positions_path -%>"><%= t('ui.position') -%></a></li>
    </ul>
  </div>
  <div class="row">
    <div class="col-md-12">
      <div class="tile">
        <div class="tile-body">
          <table id="positions-datatable" class="table table-hover table-bordered" data-source="<%= positions_path(format: :json) -%>">
            <thead>
              <tr>
                <th>ID</th>
                <th><%= t('position.name') -%></th>
                <th><%= t('position.functional_category') -%></th>
                <th><%= t('ui.admin_action') -%></th>
              </tr>
            </thead>
            <tbody>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</main>
